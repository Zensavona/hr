<h1>Password Reset</h1>
<%= form_for @changeset, @path, fn f -> %>
  <%= if f.errors != [] do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check out the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_input f, :id, value: @changeset.changes.id %>
  <%= hidden_input f, :password_reset_token, value: @changeset.changes.password_reset_token %>
  <div class="form-group">
    <%= password_input f, :password, placeholder: "New Password", class: "form-control" %>
  </div>
  <%= submit "Reset My Password", class: "btn btn-primary" %>
<% end %>
